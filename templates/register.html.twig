{% extends "layout.html.twig" %}

{% block content %}
    {% form_theme form "bootstrap_3_layout.html.twig" %}
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3 form-border">
            <div class="col-md-12 col-xs-12" align="center">
                <div class="line"><h3>{{ "now"|date("d/m/Y H:i") }}</h3></div>
                  
                <h1>Hi Guest</h1>
               
            </div>
                {{ form (form) }}
            </div>
        </div>
    </div>
{% endblock %}
