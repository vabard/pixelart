{% extends 'admin/layout_admin.html.twig'%}

{% block content %}
        <div class="col-md-6">
            <div class="card">
                <div class="header">
                    <h4 class="title">Connexion à l'espace administrateur</h4>
                </div>
                <div class="content">
                    <form class="form" method="post" action="{{ path('admin_login_check') }}">
                        <div class="row">
                            <div class="col-md-6 col-md-offset-2">
                                <div class="form-group">
                                    <label>Pseudo</label>
                                    <input type="text" class="form-control" name="_username" placeholder="" value="">
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 col-md-offset-2">
                                <div class="form-group">
                                    <label>Mot de passe</label>
                                    <input type="password" class="form-control" name="_password" placeholder="" value="">
                                </div>
                            </div>
                        </div>
                        {% if error %}
                        <div class="alert alert-danger">
                            {{ error | trans }}
                        </div>
                        {% endif %}
                            <button type="submit" class="btn btn-info btn-fill pull-right">Connexion</button>
                        <div class="clearfix"></div>
                    </form>
                </div>
            </div>
        </div>{# empty Twig template #}
{% endblock %}